server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NeySistemas/BootCampBC21.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
